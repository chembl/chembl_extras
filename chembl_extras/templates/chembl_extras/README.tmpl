***************************************************************************
*
* Title:        ChEMBL FTP Directory
*
* Release:      {{ release_version }}
*
* Date:         {{ release_date|date:"d/m/Y" }}
*
***************************************************************************

The data content in ChEMBL is licensed under a highly permissive Creative
Commons license - specifically the "CC Attribution-ShareAlike 3.0 Unported
license", see LICENSE file. The required attribution should contain the url
of the ChEMBL resource, and also the release version, e.g.:

ChEMBL data is from http://www.ebi.ac.uk/chembl - the version of ChEMBL is
{{ release_version }}.

This should be visible on the entry portal for a web resource in which
ChEMBL is integrated, or contained with the documentation for any further
distribution.


Contents of this directory:
---------------------------

README                               This file

LICENSE                              License data is released under: Creative
                                     Commons Attribution-Share Alike 3.0
                                     Unported License

{{ release_version }}_release_notes.txt          {{ release_version }} release details, which includes
                                     counts and schema change information

{{ release_version }}_schema_documentation.html  Listing of all {{ release_version }} tables and
                                     columns in html format

{{ release_version }}_schema_documentation.txt   Listing of all {{ release_version }} tables and
                                     columns in plain text format

{{ release_version }}.fa.gz                      Fasta file of proteins in {{ release_version }}
                                     target_dictionary table

{{ release_version }}_bio.fa.gz                  Fasta file of the sequences of proteins
                                     in the {{ release_version }} biotherapeutics table

{{ release_version }}.sdf.gz                     SDF of {{ release_version }} compounds, includes
                                     chembl_id

{{ release_version }}_chemreps.txt.gz            Tab-separated file containing different
                                     chemical representations (SMILES, InChI
                                     and InChI Key) of {{ release_version }} compounds,
                                     includes chembl_id

{{ release_version }}_monomer_library.xml        {{ release_version }} HELM monomer library

{{ release_version }}_schema.png                 Schema diagram of {{ release_version }} database

{{ release_version }}_mysql.tar.gz               Files required to load {{ release_version }} data
                                     into a MySQL database

{{ release_version }}_oracle9i.tar.gz            Files required to load {{ release_version }} data
                                     into a Oracle 9i database

{{ release_version }}_oracle10g.tar.gz           Files required to load {{ release_version }} data
                                     into a Oracle 10g database

{{ release_version }}_oracle11g.tar.gz           Files required to load {{ release_version }} data
                                     into a Oracle 11g database

{{ release_version }}_postgresql.tar.gz          Files required to load {{ release_version }} data
                                     into a PostgreSQL database

{{ release_version }}_sqlite.tar.gz              Files required to load {{ release_version }} data
                                     into a SQLite database

chembl_uniprot_mapping.txt           Mapping between {{ release_version }} target
                                     chembl_ids and UniProt accessions


